import styled from "styled-components";

export const PrimaryButton = styled.button`
  padding: 8px 12px;
  border-radius: 8px;
  background-color: #1DA1F2;
  border: none;
  width: auto;

  color: #FFFFFF;
  font-weight: 600;
  font-size: 14px;

  cursor: pointer;

  &:hover {
    opacity: 0.8;
  }
`